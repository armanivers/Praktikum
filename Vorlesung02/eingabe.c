#include <stdio.h>

int main(){
    long zahl;
    scanf("%ld",&zahl);
    printf("%ld",zahl);
    // Bei der Eingabe von 12hallo wird nur die "12" gespeichert und die restliche Character dahinter ignoriert 
    return 0;
}